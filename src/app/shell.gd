extends Control

func _ready() -> void:
	# Presentation is intentionally non-authoritative. Phase 12A only proves that
	# the project has a runnable shell while deterministic systems live outside Nodes.
	print("Organism Cargo bootstrap shell ready")
